# Secrets — infrastructure credentials; do NOT commit a populated .env
# User preferences moved to ~/.config/argos/config.toml (see config.toml.example)

# PostgreSQL
POSTGRES_USER=argos
POSTGRES_PASSWORD=argos_dev_password
POSTGRES_DB=argos
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Slack (Socket Mode)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
